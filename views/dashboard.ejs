<%- include("layout", {
  user,
  body: `
  <div class="card">
    <h2>Welcome ${user.name}</h2>
    <p>You have:</p>
    <ul>
      <li>${rideCount} rides created</li>
      <li>${bookingCount} rides booked</li>
    </ul>
  </div>
`}) %>
