<div class="card">
  <h2>🚘 ${ride.start_location} → ${ride.end_location}</h2>

  <p><strong>Driver:</strong> ${ride.driver_name}</p>
  <p><strong>Contact:</strong> ${ride.driver_email}</p>
  <p><strong>Time:</strong> ${ride.departure_time}</p>

  <p><strong>Price:</strong> ₹${ride.price_per_seat} / seat</p>
  <p><strong>Total:</strong> ₹<span id="total">0</span></p>

  <input type="number" name="seats" id="seatInput"
         min="1" max="${ride.available_seats}" />

  <a class="book-btn" href="/confirm?id=${ride.id}">Confirm Booking</a>
</div>
